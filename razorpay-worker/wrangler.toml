name = "nosh7-pay"
main = "worker.js"
compatibility_date = "2024-11-01"

# If `wrangler deploy` says "Failed to automatically retrieve account IDs",
# paste your Cloudflare Account ID here (Dashboard > Workers & Pages > Account ID
# on the right side). It is not a secret. Then run `wrangler deploy` again.
# account_id = "your_account_id_here"

[vars]
ALLOW_ORIGIN = "https://start.nosh7.in"
